# TODO — 車載ドライバーモニタリングに向けたルールベース改善

最終目標：車に載せ、ドライバーの危険状態（眠気・注意逸脱）を判定して警告フィードバックを返す。
当面は ML ではなくルールベースで精度を詰める。追加ハードは使わない（既存カメラのみ）。

## いま残っているのはデータ待ちの5件だけ

コードで進められることは一巡した。ここから先は**録らないと進まない**。

- [ ] しきい値を収録データで詰める（blink_dynamics / nodding / respiration / blink_rate）
- [ ] 公開データセットでのしきい値チューニング（NTHU-DDD / UTA-RLDD / DMD）
- [ ] HRV 修復の検証に PhysDrive（車載 rPPG 公開データセット）を使う
- [ ] 実車でミラー・メーターの角度を測り `zones.enabled: true` にする
- [ ] 視線しきい値の較正（`gaze_off.off_threshold` / `gaze_scanning.center_radius` /
      `_zones.forward_gaze`）。`gaze_x` が左右の動きに反応するようになったので、
      これらは初めて意味を持つ。いまの 0.035 は視線ズレが常に 0 だった頃の値で根拠が無い

**次に録るべきもの（優先順）**
1. 安静2分→暗算2分→回復2分（`collect.bat --protocol stress`）。HRV の実データ検証。
   `rppg.hrv_enabled` は既に true なので、これは有効化の条件ではなく答え合わせ。
   RMSSD が安静で 20〜50ms に収まり、暗算で下がることを確かめる。
2. 単調な映像を見ながらの長時間収録。眠気の早期兆候（瞬きの遅さ・うなずき）の較正。
3. ミラー位置の目印を見る指示を入れた収録。視線ゾーンの角度決め。ここで視線の
   しきい値もまとめて較正できる（左右を見る指示が入るので）。

## iPhone 経路（映像入力と警告出力）

- [x] iPhone アプリからの WebSocket 映像入力と結果返送
      `sources/iphone_ws.py`（IPhoneLink + IPhoneSource）と `feedback/iphone_ws.py`
      （IPhoneSink）。PC がサーバ、端末がクライアント、既定ポート 8765。
      1 メッセージ = 撮影時刻(float64 LE 8バイト) + JPEG。撮影時刻は端末の値をそのまま
      `Frame.timestamp` に入れる（受信時刻で代用すると通信の揺らぎが生理指標を歪める）。
      映像と結果で同じ接続を使うので、接続は IPhoneLink が持ち source と sink が共有する。
      `config.source.type: iphone` / `feedback.iphone` で切り替え。
      端末が無くても `examples/iphone_fake_device.py` で経路全体を確かめられる。
- [x] 繋ぎ直しで判定が止まっていたのを直す
      端末を繋ぎ直すと撮影時刻が 0 付近へ戻り、時刻が戻ったフレームを渡された
      MediaPipe の `detect_for_video` が例外を投げて、以降のフレームも全部そこで落ちて
      いた。1 回の切断で判定が二度と戻らない（「待ち受けを続けます」と出たまま無警告）。
      `IPhoneLink._stamp` で区間の境目だけをずらす。区間の中は端末の値のままなので
      まばたき率・あくび間隔・rPPG は影響を受けない。切れていた時間は PC の時計で測る。
- [ ] 実機での確認（ファイアウォールで 8765 の受信を許可 →
      端末に `ws://<PCのIPアドレス>:8765` を入れる → 送信枚数と警告の往復を見る）
- [ ] 実機のフレーム到達率と遅延を測る。無線が詰まったときに何が落ちるかを見る

## 優先度高（すぐ効く・バグに近い）

- [x] `gaze_x` が左右の視線に反応しなかったのを直す
      目頭→目尻の比を両目で平均していた。左右を見たとき両目の虹彩は画像上で同じ向きへ
      動くが、目頭と目尻の並びが左右の目で鏡像なので比は逆向きに出て、平均が打ち消し
      合っていた（合成入力で確認：視線を ±0.012 動かしても `gaze_x` は 0.500000 のまま）。
      基準を目頭ではなく目の中央に取り、幅を絶対値にして向きに依らせないようにした。
      これで gaze_off / gaze_scanning / attention_buffer の視線側が初めて効く。
      **`model.pkl` は作り直しになる**（学習時 `gaze_x` は定数だった）。

- [x] PERCLOS をフレーム割合 → 時間重み付き割合にする
      `_support.py` の `true_fraction` を `time_fraction` に置き換えた。標本が代表する
      時間で重み付けし、大きく空いた間隔は中央値の2倍で頭打ちにする
      （見失った時間を直前の1標本の重みに丸ごと足さないため）。
      eye_closure / head_down / head_turn / gaze_scanning の PRC に反映済み。
- [x] 窓内カバレッジのゲートを入れる
      `window_coverage` を追加。分母は窓の公称長ではなく履歴に実際にある時間幅
      （公称長で割ると起動直後に全 cue が黙る）。eye_closure / gaze_scanning /
      blink_dynamics / nodding が `min_coverage` 未満で valid=False を返す。
- [x] EMA を非対称にする（立ち上がり速く・解除遅く）
      `attack_frames: 2` / `release_frames: 20`。上げと下げで別の係数を使う。
- [x] レベル境界に真のヒステリシスを入れる
      `LevelLatch`。上げは境界そのまま、下げは `release_margin`(既定 0.08) ぶん
      下回るまで待つ。境界に張り付いた入力での警告音の鳴り止み・鳴り直しを防ぐ。

## 優先度中（検出の感度・網羅性）

- [x] 瞬き持続時間 + まぶた再開速度の cue を追加（`blink_dynamics`）
      `_episodes.py` で閉眼エピソードを切り出す（入口/出口で別しきい値。1本だと
      境界の震えで1回の瞬きが複数に割れる）。閉眼の長さと戻りの遅さは同じ生理から
      来るので max を取る（足すと二重計上になる）。
- [x] うなずき（nodding）検出 cue を追加
      落ちて戻る往復だけを数える。戻らなければ居眠り姿勢なので head_down に任せる。
- [x] 「顔なし」自体を警告にする（`face_absent`）
      他の cue が全部黙る状況で唯一声を出す。grace_seconds で単発の検出漏れは無視。
- [x] 警告の段階設計
      MEDIUM は先頭の一音を小さく一定間隔（急かさない）、HIGH はパターン全体を鳴らし、
      続く間は鳴らすたびに間隔を詰める（`escalate_factor` 0.7、下限 1.5 秒）。
      段が上がったら間隔を待たずに鳴らす。収まったら次は最初の1回から数え直す
      （収まりを伝えないと、次の警告が前の続きとして詰めた間隔から始まる）。
- [ ] しきい値を収録データで詰める
      blink_dynamics の normal/drowsy、nodding の amplitude/回数は文献の代表値を
      初期値に置いただけ。自前収録＋公開データセットで較正する。

## 優先度中（車載ドメイン対応の本丸）

- [x] 視線ゾーンの導入（`_zones.py`）
      前方 / ミラー / メーター / 脇見に分け、ゾーンごとに減り始めるまでの猶予を変える。
      猶予はミラー確認の実際の長さ（0.6〜1.0秒）より短くしてある。全部を無罰にすると
      短い確認を繰り返して前方を見ない運転（視覚的時分割）を素通りさせるため。
      下向きの判定に上限（`instrument_pitch_max`）を置き、膝元のスマホがメーター確認に
      化けるのを防ぐ。`gaze_dx`（符号付き視線ズレ）を normalize に追加。
      **既定では無効**。ミラーとメーターの角度は車種・シート位置・取り付けで変わり、
      測らずに既定値を当てると「脇見をミラー確認と読む」ことになって区別しないより危険。
      → 残作業: 実車で角度を測り、`zones.enabled: true` にして較正する。
- [x] 前方基準の自動推定（`_forward.py`・`auto_forward`）
      走行中の分布の**最頻値**を前方とみなす。中央値だと片側のミラーをよく見る癖で
      そちらへ引かれる。積むのは補正前の生の値（補正後を積むと、いま前方だと思って
      いる向きの標本だけが集まり最初のずれが固定される）。分布が平らなときは確定
      しない。既定は無効で、確定するまでは従来どおり動く。
- [x] サングラス・夜間の縮退モード（`_eye_health.py`）
      「1分間に瞬きが1回も無い」を目の信号が死んでいる証拠として使う（人は安静時でも
      10〜20回/分は瞬きする）。サングラス越しの EAR は低いまま張り付くので、放置すると
      目を閉じていないのに PERCLOS が満点になり、**眠気を誤って警告する**最も避けたい
      向きの誤りになる。検出したら eye_closure と blink が黙り、うなずき・うつむき・
      あくび・見失いで判定を続ける。起動直後を誤ってサングラスと読まないよう、窓が
      8割埋まるまでは「使える」を返す。
      → あわせて policy を修正: max 統合の平均も分母を「測れている cue」に限る。
      全 cue 固定の分母だと、目の cue が4本落ちた時点で残りが満点でも 3/7 にしかならず、
      縮退運転が成立しなかった。
      → 夜間そのものは iPhone の TrueDepth（赤外）で解決しうる。上の iPhone の節を参照。

## システム面

- [x] 再キャリブレーション時に cue の内部状態をリセットする
      `Resettable` ポートを追加。`Pipeline.reset_state()` が履歴・rPPG バッファ・
      cue の安静基準・注意残高・EMA・段をまとめて捨てる。'c' キーから呼ぶ。
- [x] カメラ喪失・検出器例外からの復帰
      取り込みは read 失敗で終わらせず、指数バックオフで開き直す（0.5→5 秒上限）。
      検出器の単発例外はそのフレームを飛ばして続行し、`_MAX_DETECT_FAILURES`(30) 回
      続いたら一時的な不調ではないと見て上へ返す。
- [x] ヘッドレス運転モード
      SIGINT / SIGTERM で終了を頼めるようにした（`request_stop`）。KeyboardInterrupt も
      正常な止め方として後始末する。`feedback.window: false` なら cv2 を触らない。
- [x] プロセス監視（`watchdog.py`）
      判定ループとは別スレッドで時計を持ち、`stall_seconds`(3秒) 判定が来なければ
      知らせる。止まったままなら `stall_repeat_seconds`(5秒) ごとに知らせ直す
      （1回きりだと、鳴った瞬間に気づかなければ二度と伝わらない）。復帰も1回知らせる。
      この装置の最悪の壊れ方は警告のしすぎではなく黙ることなので、外から見張る。

## 評価方法

- [x] イベント単位・誤警報率での評価を用意する（`evaluation/events.py`）
      「危険エピソードを何件・何秒遅れで捕まえたか」と「平常1時間あたり何回よけいに
      鳴ったか」を出す。フレーム単位の accuracy はどちらも表さない（収録の大半は
      平常区間なので、警告を一切出さない判定器でも高く出る）。
      遅延は中央値で見る（1回の大外れが平均を支配するため）。警告が危険より先に
      始まっていた場合の遅延は 0 に丸める（負の遅延は「早く気づけている」と読めるが、
      実際は危険の前から鳴っていた＝誤警告に近い）。
      → 残作業: `report.bat` / `evaluation/runner.py` からこの採点を呼ぶ配線。
- [ ] 公開データセットでのしきい値チューニング
      NTHU-DDD / UTA-RLDD / DMD あたりを ingest で取り込む
      （feat/dataset-converters・feat/drowsiness-annotation ブランチと合流させる）。

## ストレス検知の強化（ハード追加なしで進める）

方針：追加ハードは使わない（サーマルカメラ・IR 瞳孔計測は不採用）。
既存カメラ+rPPG の範囲で指標を増やし、マルチモーダル融合で特異度を上げる。

- [x] stress 軸の統合を3指標以上の weighted に拡張
      `min_agree`（既定2）を導入。分母を「計測できている cue」に絞り、同意本数が
      足りなければその割合まで割り引く。分母を全 cue 固定にしていた従来は、値が出ない
      cue が 0 として平均を薄め、残る cue がどれだけ強くても警告に届かなかった。
      同意は active ではなく警告の強さで数える（facial_tension は約束として
      active を立てないので、active で数えるとどれだけ揃っても 0 本になる）。
- [x] HRV のピーク検出を作り直した（`bio/peaks.py`・`bio/hrv.py`）
      **原因**: 全体を標準偏差で正規化して固定しきい値で山を採っていたため、呼吸に
      よる基線の揺れで拍を取りこぼしていた。拍を1つ落とすとその RR だけが2倍になり、
      隣接差を見る RMSSD が跳ね上がる。実収録の中央値 225ms はこれで説明がつく。
      **対策4段**: (1) 心拍の帯域だけ残す（縁は余弦でなだらかに。矩形で切ると時間領域に
      リンギングが出て窓端の拍位置が数十msずれる）、(2) しきい値を局所実効値との比に、
      (3) 高い順ではなく時間順に走査して不応期を効かせる（雑音の突起が本物の拍を
      押し退けない）、(4) 拍間隔の異常値を外してから RMSSD を出す（HRV 解析の標準作法）。
      窓の両端の拍は落とす（周波数側の処理は窓が周期的だと仮定するため）。
      **実測（合成波・真値40ms）**: 旧 223〜1210ms → 新 35〜37ms。
      低変動（真値15ms）も 20ms 前後で分離でき、安静とストレスの区別がつく。
      main 側にも独立に書き直された実装があったが、同じ合成波で真値の約1.45倍
      （真値40→59ms、15→22ms、60→86ms）に膨らんだ。拍の個数は合っていたので
      取りこぼしではなく拍位置の精度の差。誤差の小さいこちらを採った。
      → **残作業**: 確かめたのは合成波だけ。実収録（安静2分→暗算2分→回復2分）で
      RMSSD が安静 20〜50ms に収まり暗算で下がることを確かめる。
- [ ] HRV 修復の検証に PhysDrive（車載 rPPG 公開データセット）を使う
      https://arxiv.org/pdf/2507.19172 。ingest 経路に乗せられるか要調査。
- [x] 呼吸数 cue を追加（`respiration`）
      rPPG の肌色時系列の低周波成分（0.1〜0.5Hz）から呼吸数を推定する。心拍とは別の
      バッファ（30秒窓）を持つ。30回/分でも2秒に1周期しかないので、心拍用の20秒窓では
      分解能が足りず、かといって心拍の窓を伸ばすと心拍側の品質が落ちるため。
      直線の傾きを先に抜く（照明のゆっくりした変化が帯域の下端に電力を作り、抜かないと
      「呼吸6回/分」が常に最強のピークになる）。本人の安静との差で判定し、単独では
      active を立てない（会話・あくび・咳でも上がる）。
- [x] 瞬き頻度上昇の stress cue を追加（`blink_rate`）
      `_episodes.py` を流用。長い閉眼は瞬きではなく眠気なので `max_blink_seconds` で
      除く（混ぜると、眠くなるほどストレスが上がって見える）。本人の安静との差で判定。
      → 残作業: span_rpm / span_rate は暫定値。収録データで詰める。

## 構成の方向：車に載せるのはスマートフォン、PC は開発機（2026-08 決定）

「PC は開発者が使う想定。それと繋がったスマートフォンでカメラを使い、音を鳴らす」
という構成を採る。車内に置くのは端末だけになり、電源・固定・取り回しの問題が一気に減る。

### 段階

1. **スマホ＝USB カメラ（今日から動く・コード変更なし）**
   DroidCam / Iriun / Camo などが端末を標準の UVC カメラとして見せる。既存の
   `source.type: webcam` がそのまま使える。時刻も PC 側の取り込み時刻になるので
   rPPG の前提が崩れない。まずこれで机上の検証を回すのが最短。
2. **スマホ＝MJPEG ストリーム（実装済み・`source.type: mjpeg`）**
   Android の「IP Webcam」等、既製アプリが出す MJPEG over HTTP を直接読む。
   アプリを自作せずに無線／USB テザリング経由で繋がる。
   → **限界**: フレームの時刻を受信側で入れるので、通信の揺らぎがそのまま時刻の
   揺らぎになる。眠気・注意逸脱は数百 ms の揺らぎに耐えるが、rPPG の心拍・呼吸は
   実効サンプリング周波数の推定が狂って精度が落ちる。
3. **専用アプリ（生体指標まで要るなら必須）**
   撮影した瞬間の時刻をフレームに添えて送る。`Frame.timestamp` にその値を入れる
   だけで以降の層は無修正で動く（contracts の設計どおり）。
   - [ ] 撮影時刻つきのフレーム送信プロトコルを決める
   - [ ] 警告を端末へ返す経路（判定 → 端末 → 車内スピーカー）を作る。
         `FeedbackSink` を1つ足すだけで済む形にする。
   - [ ] 通信は USB テザリング推奨。車内の無線は不安定で、切れた瞬間に無警告になる。
         切れたこと自体は Watchdog が知らせる（実装済み）。
4. **全部を端末で動かす（製品方向・将来）**
   MediaPipe には Android/iOS の Tasks API がある。cue とポリシーの移植が要るので
   デモの範囲では手を出さない。contracts が数値の塊だけで出来ているのは、この分割を
   見込んでのこと。

### iPhone を使う場合（2026-08 調査）

**Mac が要るのは段階3だけ。** iOS のネイティブアプリを作るには Xcode が必要で、
Xcode は macOS でしか動かない（Apple の制約で回避できない）。ただし:

- 段階1・2 は**アプリを作らないので Mac は不要**。Camo / iVCam / EpocCam などが
  iPhone を Windows から標準の UVC カメラとして見せる（USB 接続可）。無料版は
  解像度や透かしに制限があるが、640x480 で足りる用途なので実用になる。
  ※ iPhone の「連係カメラ」は macOS 専用なので Windows では使えない。
- 段階3も、**ネイティブアプリの代わりに Safari の Web ページ**で作れば Mac は不要。
  `getUserMedia` でカメラ、WebSocket でフレーム送信、`requestVideoFrameCallback` で
  撮影時刻を添えられる（時刻の問題が部分的に解決する）。音の再生もできる。
  制約: HTTPS 必須（自己署名証明書かトンネル）、画面を消すと止まる、
  バックグラウンド動作なし、**ARKit / TrueDepth には触れない**。
- ネイティブアプリを作る場合の費用: 無料の Apple ID でも自分の端末には入れられるが、
  プロファイルが**7日で切れる**ので毎週入れ直しになる。年 $99 の Developer Program に
  入れば1年有効＋TestFlight が使える。デモ期間だけなら無料でも回せる。

**iPhone のカメラで改善できること**（大きい順）:

1. **暗所性能とセンサ雑音** — rPPG は肌色の 1% 未満の変化を拾う処理なので、センサ雑音が
   そのまま精度を決める。前面カメラの画質差は心拍・呼吸の品質に直接効く。顔検出の
   安定性も夕方以降で変わる。これは段階1・2（RGB のみ）でもそのまま得られる。
2. **露出・ホワイトバランスの固定** — 自動露出の追従は rPPG を壊す最大の要因の一つ。
   iOS は `AVCaptureDevice` で明示的にロックできるが、USB カメラの多くはできない。
   → ネイティブアプリが要る（Safari の `applyConstraints` では露出を固定できない）。
3. **TrueDepth / ARKit**（iPhone X 以降・ネイティブアプリのみ）
   - **赤外の投光器と深度で真っ暗でも顔が取れる**。「夜間は IR カメラが要る」という
     課題が、ハードを足さずに解決する。下の「サングラス・夜間の縮退モード」の
     前提が変わるので、iPhone を採るなら先にここを検討する価値がある。
   - **両目の3D視線方向**が取れる。いまの `gaze.py` は虹彩の左右位置しか見ておらず、
     `_zones.py` は縦方向の視線が無いので頭部の pitch で代用している。ARKit なら
     縦の視線が直接取れるので、メーターと膝元のスマホの区別がずっと確かになる。
   - **52 個の blendshape** が取れる。MediaPipe の blendshape は ARKit の集合に
     合わせて作られているので、`browDownLeft` などの名前がそのまま一致する。
     つまり `facial_tension` は無修正で動く。
   - 6DoF の頭部姿勢も、いまの PnP 推定より正確。

**結論**: 段階1・2 は Mac 無し・無料で今日から。最も価値が大きい改善（暗所の IR、
3D視線、露出固定）はネイティブアプリが要るので、そこまでやるなら Mac が必要。
Android 端末があるなら開発は Windows だけで完結する（ただし TrueDepth 相当は無い）。

### 音を鳴らす側

- 段階1・2 では PC が鳴らす。車内スピーカーへは Bluetooth か AUX で流す。
- 段階3以降は端末が鳴らす。段階設計（notice / warn とエスカレーション）は
  実装済みなので、鳴らす場所だけが変わる。

## デモ用ソフトの想定（PC を車に載せられない前提）

実際には載せられなくはない（ノートPC＋USBカメラをダッシュに固定するのは研究では標準的な
構成）。しかし本当の障害は電源でも固定でもなく、**眠い状態・危険な状態の運転を実車で
再現できない**こと。ゆえにデモは「実車で動かす」ではなく「判定の質を見せる」ものにする。

### 想定する3形態

1. **運転席モックアップ（本命）**
   - 机にノートPC。カメラは画面上端ではなく**ダッシュ相当の低い位置**に置き、下から
     見上げる角度にする。実車の設置に一番近く、pitch/yaw の見え方が変わるので、
     この角度でしきい値を詰めておく意味がある。
   - 別画面（またはPC画面）に前方風景の動画を流し、被験者はそれを見て「運転」する。
   - 左右の壁／モニタ脇に**ミラー位置の目印**を貼り、「ミラー確認して」と指示できる
     ようにする。視線ゾーン（上記の本丸項目）の検証がこれで回せる。
   - 眠気は演技ではなく、単調な映像＋昼食後＋暗めの照明で自然に誘発する。
     `--guided` の収録プロトコルをこの配置向けに1本足す。

2. **録画済みシナリオの再生（発表用の保険）**
   - ステージで人が眠くなるのを待てない。`--video` は既にあるので、
     「被験者映像＋期待される判定」をセットにしたシナリオを数本用意し、
     ワンコマンドで再生できるようにする。タイムライン表示が主役になる。

3. **停車中の実車で収録だけする**
   - 判定はオフライン。実車の照明（直射日光・トンネル・夜）とシート振動が
     rPPG と顔検出をどこまで壊すかを見る。リアルタイム実車デモは電源・固定・安全の
     手間の割に得るものが少ないので、収録に割り切る。

### デモのために本体へ足すもの

- [x] 画面を「運転者向け」と「観客向け」に分ける（`feedback.driver_view: true`）
      運転者向けは別窓「Driver」に警告だけ。警告が無い間は黒のまま（夜間に明るい画面を
      正面に置くのは、それ自体が危険）で、動いていることを示す小さな点だけを出す。
      MEDIUM は橙の枠と軸名、HIGH は赤の枠に "PULL OVER" を足す。
      観客向けの窓はこれまでどおり特徴量・cue の内訳・タイムラインを全部出す。
- [x] 警告は音だけで完結させる
      段階を音の形（notice=先頭の一音を小さく / warn=パターン全体）で分け、HIGH が続く
      間は間隔を詰める。上の「警告の段階設計」で実装済み。
- [x] シナリオ再生モード（`--scenario <manifest.json>`）
      取り込み用の manifest（動画＋区間ごとの軸別ラベル）をそのまま使う。書式を増やす
      理由がないため。動画を**実時間で**流し（既定の最速再生では判定より先に映像が
      終わる）、画面右上に「その時刻に何が起きているはず」を判定と並べて出す。
      再生中は自動で録画するので、終わったらそのまま `report.bat` で採点できる。
